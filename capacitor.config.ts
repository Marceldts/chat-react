import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitorjs.app',
  appName: 'Chat en React',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
